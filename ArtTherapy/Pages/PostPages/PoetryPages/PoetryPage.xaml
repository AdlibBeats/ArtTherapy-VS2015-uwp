<Page
    x:Class="ArtTherapy.Pages.PostPages.PoetryPages.PoetryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:l="using:ArtTherapy"
    xmlns:local="using:ArtTherapy.Pages.PostPages.PoetryPages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:data="using:ArtTherapy.ViewModels"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls.WrapPanel"
    xmlns:Interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:Interactions="using:Microsoft.Xaml.Interactions.Core"
    mc:Ignorable="d" Loaded="Page_Loaded">
    
    <Grid Background="{StaticResource Static.Grid.Background}">
        <ScrollViewer Name="scrollViewer" ViewChanged="ScrollViewer_ViewChanged">
            <Grid Background="White">
                <GridView ItemsSource="{Binding PostModel.Items}">
                    <GridView.ItemTemplate>
                        <DataTemplate>
                            <Grid Height="200" Width="175" Margin="5" Background="White">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="10*"/>
                                    <RowDefinition Height="2*"/>
                                    <RowDefinition Height="2*"/>
                                    <RowDefinition Height="1*"/>
                                    <RowDefinition Height="1*"/>
                                </Grid.RowDefinitions>
                                <ProgressRing Height="50" Width="50" IsActive="{Binding IsLoading}" BorderBrush="Gray" Foreground="Gray" Grid.RowSpan="5" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                <Border Grid.Row="0" Visibility="{Binding Visibility}">
                                    <Border.Background>
                                        <ImageBrush ImageSource="{Binding Image}" Stretch="UniformToFill"/>
                                    </Border.Background>
                                </Border>
                                <Border Grid.Row="1" Background="LightGray" Visibility="{Binding Visibility}">
                                    <TextBlock FontSize="12" HorizontalAlignment="Left" VerticalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap" Text="{Binding Name}"/>
                                </Border>
                                <Grid Grid.Row="2" Background="White" Visibility="{Binding Visibility}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" FontSize="12" Text="{Binding Description}"  TextWrapping="Wrap"/>
                                    <Button Grid.Column="1" FontSize="12" Width="50" Height="50" Content="Buy" Background="Red"/>
                                </Grid>
                                <TextBlock Grid.Row="3" FontSize="12" HorizontalAlignment="Left" VerticalAlignment="Center" TextAlignment="Center"  TextWrapping="Wrap" Text="{Binding Id}" Visibility="{Binding Visibility}"/>
                                <TextBlock Grid.Row="4" FontSize="12" HorizontalAlignment="Left" VerticalAlignment="Center" TextAlignment="Center"  TextWrapping="Wrap" Text="{Binding Type}" Visibility="{Binding Visibility}"/>
                            </Grid>
                        </DataTemplate>
                    </GridView.ItemTemplate>
                </GridView>
            </Grid>
        </ScrollViewer>
    </Grid>
</Page>
